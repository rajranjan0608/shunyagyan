import { Route, Routes as MRoutes, useNavigate } from 'react-router-dom';


export default function Routes(){
  const navigate = useNavigate();

  return(
    <MRoutes>
      <Route>
        
      </Route>
    </MRoutes>
  );
}